# DEPRECATED docs

DEPRECATED: not used in current canonical workflow; kept only for reference.

Replacement: use `scripts/weekly_update.ps1`, `scripts/publish_public.ps1`, and `src/tatemono_map/building_registry/`.
